%include <qsub_build.h>
%include <head.h>
%manual
Run get_metstate.py.
Outputs are saved in the Met_Loc defined in config file.
Only run if a new state file needs to be created. Otherwise, use existing state.
NC_ENVIRON is a conda environment with working cdo and nco tools and bindings.
%end

source activate %NC_ENVIRON%
export CDO="%NC_ENVIRON%/bin/cdo"

echo "Today is $(date)"

python %ECF_HOME%/processes/main/meteorological/get_BC_metstate.py %WORKING_CFG_FILE%


%include <tail.h>
