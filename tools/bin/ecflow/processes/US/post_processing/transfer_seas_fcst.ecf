#!/bin/bash
%include <qsub_build.h>
%include <head.h>
%manual
Creates netCDFs with percentiles for soil moisture, swe, 
and soil moisture + swe (total moisture). 
Transfers the netCDFs to the NKN network. 
%end

echo "Today is $(date)"

python %ECF_HOME%/processes/main/post_processing/file_transfer.py %WORKING_CFG_FILE% MONITOR

%include <tail.h>

