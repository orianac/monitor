%include <qsub.h>
%include <head.h>
%manual
This script downloads meteorological data from
http://thredds.northwestknowledge.net/
using wget. 
It also checks to see if the downloaded data is different
from the previous day because on the date file. 
Outputs are saved in %MET_LOC% as defined in config file
%end

echo "Today is $(date)"

python %ECF_HOME%/processes/main/meteorological/get_metdata.py '%CFG_FILE%' '%MET_LOC%'


%include <tail.h>
