#!/bin/bash
%include <qsub.h>
%include <head.h>
%manual
Changes kelvin to celcius
Checks that all the parameters are the same length and adjusts if they are not
Combine the individual ascii files for precip, tmax, tmin, and wind 
for each grid cell
Outputs are saved in %MET_LOC% as defined in config file
%end

echo "Today is $(date)"

python %ECF_HOME%/processes/main/meteorological/combine.py '%MET_LOC%'

%include <tail.h>
