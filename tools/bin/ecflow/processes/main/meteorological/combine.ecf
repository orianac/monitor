#!/bin/bash
%include <qsub.h>
%include <head.h>
%manual
Changes kelvin to celsius
Combine the individual ascii files for precip, tmax, tmin, wind, 
specific humidity and SW radiation for each grid cell.
Outputs are saved in Final_Met_dir as defined in config file
%end

echo "Today is $(date)"

python %ECF_HOME%/processes/main/meteorological/combine.py %WORKING_CFG_FILE%

%include <tail.h>
