#!/bin/bash
%include <qsub.h>
%include <head.h>
%manual
Runs vic 4.2 to disaggregate forcings 
with OUTPUT_FORCE=TRUE, using the multiprocessor
%end

echo "Today is $(date)"

python %ECF_HOME%/processes/main/models/subdaily_disaggregate.py %WORKING_CFG_FILE%

%include <tail.h>
